---
# Defaults del rol common — pueden sobreescribirse con:
#   ansible-playbook -i inventory.ini playbooks/site.yml -e environment_name=prod
app_dir: /home/ubuntu/app-jardin
environment_name: dev
aws_region: sa-east-1
